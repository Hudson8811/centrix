extends templates/main
block variables
	- let pageTitle = TITLE_PART + ' | Vertical projects';
	- let isHome = false; //- Если true то логотип не будет ссылкой
	- let currentPage = 1.05; // Для каждой страницы уникален. Номер смотреть в файле pug/templates/_vars.pug в массивах с пунктами меню
	- let currentSection = 1; // // Для каждой раздела уникален. Home -1, projects - 2, news - 3, pages - 4
	- let headerClasses = 'header--absolute';
	//- let socialModClass = '';
	//- let navigationModClass = 'navigation--hover-white';
	- let isLeftwardHeader = true;
	- let bodyModClass = 'webpage--bg-white';

block header
	include modules/header

block content
	include modules/projects-listing

block footer
	include modules/footer
